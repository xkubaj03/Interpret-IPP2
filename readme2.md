#Implementaèní dokumentace k 2. úloze do IPP 2021/2022
##Jméno a pøíjmení: Josef Kuba
##Login: xkubaj03
Skript interpret.py má za úkol provést Instrukce zadané ve vstupním xml souboru, kterı je vıstupem pøedchozího úkolu parse.php. Moji implementaci jsem pro pøehlednost rozdìlil do dvou souborù interpret.py a Instructions.py. Moje implementace obsahuje rozšíøení NVI (aplikování objektovì orientovaného programování), díky èemu jsem dosáhl zvıšení pøehlednosti kódu.
Soubor Instructions.py obsahuje jednotlivé tøídy pouívané interpret.py. Za zmínku rozhodnì stojí tøída Instruction, která obsahuje (nahrané) pole vstupù, seznam instrukcí, aktuální pozice v programu (ProgramIndex), globální rámec, zásobník ve kterém jsou uloeny lokální rámce, doèasnı rámec (Temporary frame), zásobník pozic (pouívanı instrukcemi CALL a RETURN), seznam návìští, zásobníkem hodnot a promìnnımi ve kterıch je uloeno zda jsou definovány lokální a doèasnı rámec (TF). Dále pro jednotlivé instrukce ukládá opcode a poøadí. Zde jsou dùleité metody get_symb a get_memory. Metoda get_symb vrátí hodnotu a typ a u jde o konstantu nebo promìnnou. Metoda get_memory vrátí ukazatel na uzel v pamìti do kterého budeme ukládat. 
Tøídy pojmenovány po jednotlivıch opcodech dìdí z tøídy Instruction a obsahují argumenty potøebné k své funkènosti. Dále také obsahují metodu execute, která provede instrukci (s jejími parametry).
Tøída Factory má metodu resolve, která je pouitá pro zvolení správného konstruktoru, jemu pøedá parametry. Pokud není nalezena pøíslušná tøída, ukonèí skript s pøíslušnım chybovım návratovım kódem. 
Soubor interpret.py nejprve zkontroluje poèet parametrù, poté jejich obsah a rozhodne, zda se má vypsat nápovìda nebo se mùe pokraèovat. Dále se pokusí otevøít soubory se zdrojem (source) a vstupem (input). Poté naète pomocní knihovny (xml.etree.ElementTree) jednotlivé parametry, které dále pøedá factory, která vytvoøí pøíslušnı objekt. Nakonec se volá na naètené instrukce metody execute èím dojde k jejich provedení.
